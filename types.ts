export interface BlogPost {
  id: number;
  title: string;
  date: string;
  summary: string;
  imageUrl: string;
  category: string;
}

export interface Feature {
  title: string;
  description: string;
  icon: string;
}